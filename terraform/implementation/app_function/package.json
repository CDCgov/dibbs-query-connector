{
  "name": "app-func",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "copy:functions": "cp -r src/functions/ProcessHL7/function.json dist/src/functions/ProcessHL7/",
    "copy:root": "cp host.json dist/ && cp package.json dist/ && cp package-lock.json dist/ && cp src/index.js dist/",
    "build": "npm run compile && npm run copy:functions && npm run copy:root",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
